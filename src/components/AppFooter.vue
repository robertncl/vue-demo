<template>
  <footer
    style="
      border-top: 1px solid var(--acme-color-border);
      background: var(--acme-color-surface-raised);
      padding: var(--acme-space-6);
    "
  >
    <div
      style="
        width: 100%;
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        gap: var(--acme-space-4);
        flex-wrap: wrap;
        font-size: var(--acme-text-sm);
        color: var(--acme-color-text-muted);
      "
    >
      <p style="margin: 0">Wanderlog — a travel planner built on the ACME design system.</p>
      <p style="margin: 0">Prices are converted from USD at fixed reference rates.</p>
    </div>
  </footer>
</template>
